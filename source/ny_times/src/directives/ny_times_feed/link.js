(function () {
    var Link = function (scope, element) {
        element.on('$destroy', function() {
        });

    };

    define([],
        function () {
            return Link;
        }
    );
}());